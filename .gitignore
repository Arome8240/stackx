node_modules
.turbo
.next
dist
build
expo-dist
.expo
.expo-shared
.DS_Store
*.log
*.tmp
coverage

# ── Environment / secrets (never commit these) ────────────────────────────────
.env
.env.*
!.env.example
!.env.*.example
!.env.sample
!.env.*.sample

# ── Deployment env snapshots ──────────────────────────────────────────────────
deployments/latest-*.env

# ── Key extraction script (contains instructions that could be misused) ───────
scripts/extract-key.mjs
scripts/extract-key.js

# ── Compiled TypeScript deploy artifacts (use .mjs versions instead) ─────────
scripts/deploy-contract.js
scripts/rollback-contract.js


