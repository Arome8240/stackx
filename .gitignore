node_modules
.turbo
.next
dist
build
expo-dist
.expo
.expo-shared
.DS_Store
*.log
*.tmp
coverage
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.testnet
.env.mainnet

# Deployment artifacts (contain contract addresses and tx IDs, safe to commit)
# but exclude any env snapshots that may contain secrets
deployments/latest-*.env
scripts/extract-key.mjs


